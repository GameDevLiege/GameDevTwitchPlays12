using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RotateGlasses : MonoBehaviour {

    public float m_speedRotation = 1f;

    // Update is called once per frame
    void Update () {
       // transform.rotation += new Quaternion(transform.rotation.x, m_speedRotation * Time.deltaTime, transform.rotation.z, transform.rotation.w);
	}
}
